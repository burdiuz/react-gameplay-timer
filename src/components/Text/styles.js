/*
 * @flow
 */

import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  text: {
    fontSize: 18,
    fontFamily: 'OpenSans',
  },
  heading: {
    textAlign: 'center',
    textAlignVertical: 'center',
    fontWeight: '400',
    fontSize: 24,
    color: '#666666',
  },
});

export default styles;
